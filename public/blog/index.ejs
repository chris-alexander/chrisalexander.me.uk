<h1>Articles</h1>

<ul>
  <% for (var slug in public.blog._data) { %>
    <% var article = public.blog._data[slug]; %>
    <% if (slug !== "index" && article.published !== false) { %>
      <li>
        <a href='/blog/<%= slug %>'><%= article.title %></a>
      </li>
    <% } %>
  <% } %>
</ul>
